
import './App.css';
import 'bootstrap/dist/css/bootstrap.min.css';

//1. create a initialState
  

function App (){
  return (
    //3. use hook useReducer
    <div className="App">
      <h1>Google Translate</h1>
      {/* <button onClick={() => {
        dispatch({type: 'SET_FROM_LANGUAGE', payload: 'es'})
      }}>Cambiar a españo</button> */}
    </div>
  )
  }
export default App
